apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: cassandra2
  labels:
    app: cassandra2
spec:
  serviceName: cassandra2
  replicas: 2
  selector:
    matchLabels:
      app: cassandra2
  template:
    metadata:
      labels:
        app: cassandra2
        interface: cassandra2
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: dc
                operator: In
                values:
                - DC2
      containers:
      - name: cassandra
        image: cassandra:3
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 7000
          name: intra-node
        - containerPort: 7001
          name: tls-intra-node
        - containerPort: 7199
          name: jmx
        - containerPort: 9042
          name: cql
        livenessProbe:
          exec:
            command:
            - cqlsh
            - -e
            - 'DESCRIBE KEYSPACES'
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: 9042
          initialDelaySeconds: 15
          periodSeconds: 10
        securityContext:
          capabilities:
            add:
              - IPC_LOCK
        lifecycle:
          preStop:
            exec:
              command: 
              - /bin/sh
              - -c
              - nodetool drain
        env:
          - name: MAX_HEAP_SIZE
            value: 512M
          - name: HEAP_NEWSIZE
            value: 100M
          - name: CASSANDRA_SEEDS
            value: "cassandra1-0.cassandracluster.svc.cluster.local,cassandra2-0.cassandracluster.svc.cluster.local"
          - name: CASSANDRA_CLUSTER_NAME
            value: "CassandraK8s"
          - name: CASSANDRA_DC
            value: "DC2"
          - name: CASSANDRA_RACK
            value: "K8sRack"
          - name: CASSANDRA_ENDPOINT_SNITCH
            value: GossipingPropertyFileSnitch
          - name: POD_IP
            valueFrom:
              fieldRef:
                fieldPath: status.podIP
        volumeMounts:
        - name: cassandra-data
          mountPath: /var/lib/cassandra/data
  volumeClaimTemplates:
  - metadata:
      name: cassandra-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      storageClassName: fast
      resources:
        requests:
          storage: 1Gi
---
kind: StorageClass
apiVersion: storage.k8s.io/v1
metadata:
  name: fast
provisioner: k8s.io/minikube-hostpath
parameters:
  type: pd-ssd

